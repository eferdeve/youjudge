{{ form_start(form) }}
    <input type="hidden" value="{{jeux.id}}" name="idJeu">
    {{ form_widget(form) }}
    <button class="myButton">{{ button_label|default('Envoyer') }}</button>
{{ form_end(form) }}



