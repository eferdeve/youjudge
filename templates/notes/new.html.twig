<!DOCTYPE html>

<title>New Notes</title>

{% block body %}
    <h1>Create new Notes</h1>

    {{ include('notes/_form.html.twig') }}

    <a href="{{ path('liste') }}">back to list</a>
{% endblock %}
