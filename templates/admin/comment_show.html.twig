{% extends 'baseback.html.twig' %}

{% block title %}Administration commentaire{% endblock %}

{% block body %}
    <h1>Commentaire de {{ commentaire.id }}</h1>

    <table id="dataTable" class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire.contenu }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ commentaire.createdAt ? commentaire.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="container">
        <div class="row">
            <div class="col-sm">
                <a class="badge badge-primary" href="{{ path('commentsList') }}">Revenir à la liste des commentaires</a>
            </div>
            <div class="col-sm">
            {{ include('commentaires/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
